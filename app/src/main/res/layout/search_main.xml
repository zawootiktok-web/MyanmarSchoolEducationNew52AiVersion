<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:ads="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/distinctive_canvas"
    android:orientation="vertical">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/nnl_appbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/distinctive_navy"
        android:elevation="3dp">
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/distinctive_navy"
            android:theme="@style/ThemeOverlay.AppCompat.Dark"
            app:popupTheme="@style/ThemeOverlay.AppCompat.Light">
            <ImageView
                android:id="@+id/ivToolbarBack2"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_gravity="start|center_vertical"
                android:contentDescription="@string/project_action_image"
                android:onClick="_Back_Click"
                android:padding="12dp"
                android:scaleType="centerInside"
                android:src="@drawable/back" />
            <TextView
                android:id="@+id/tvViewerTitle"
                style="@style/TextAppearance.AppCompat.Widget.ActionBar.Title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:fontFamily="@font/mo_re"
                android:text="@string/app_name"
                android:textColor="@color/distinctive_card_surface" />
        </androidx.appcompat.widget.Toolbar>
    </com.google.android.material.appbar.AppBarLayout>

    <ImageView
        android:id="@+id/ivToolbarBack"
        android:layout_width="match_parent"
        android:layout_height="72dp"
        android:contentDescription="@null"
        android:paddingStart="@dimen/project_screen_padding"
        android:paddingEnd="@dimen/project_screen_padding"
        android:scaleType="centerInside"
        android:src="@drawable/search" />

    <LinearLayout
        android:id="@+id/search_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/distinctive_teal"
        android:orientation="horizontal"
        android:paddingStart="@dimen/project_screen_padding"
        android:paddingEnd="@dimen/project_screen_padding"
        android:paddingTop="@dimen/project_item_spacing"
        android:paddingBottom="@dimen/project_item_spacing">

        <androidx.cardview.widget.CardView
            android:id="@+id/rl"
            android:layout_width="0dp"
            android:layout_height="56dp"
            android:layout_weight="1"
            app:cardBackgroundColor="@color/project_surface"
            app:cardCornerRadius="18dp"
            app:cardElevation="1dp">
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:paddingStart="12dp"
                android:paddingEnd="4dp">
                <androidx.cardview.widget.CardView
                    android:id="@+id/search_bar"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1"
                    app:cardBackgroundColor="@color/project_surface"
                    app:cardCornerRadius="18dp"
                    app:cardElevation="0dp">
                    <EditText
                        android:id="@+id/et"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:background="@android:color/transparent"
                        android:fontFamily="@font/mo_re"
                        android:hint="Search "
                        android:imeOptions="actionSearch"
                        android:maxLines="1"
                        android:paddingStart="4dp"
                        android:paddingEnd="4dp"
                        android:singleLine="true"
                        android:textColor="@color/project_on_surface"
                        android:textColorHint="@color/project_on_surface_muted"
                        android:textSize="@dimen/project_text_body" />
                </androidx.cardview.widget.CardView>
                <ImageButton
                    android:id="@+id/bt_mic"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:background="@android:color/transparent"
                    android:contentDescription="@string/project_action_image"
                    android:padding="12dp"
                    android:scaleType="centerInside"
                    android:src="@drawable/ic_search_black"
                    android:tint="@color/project_primary" />
            </LinearLayout>
        </androidx.cardview.widget.CardView>

        <ImageView
            android:id="@+id/filter"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginStart="8dp"
            android:background="@color/project_surface"
            android:contentDescription="@string/project_action_image"
            android:padding="12dp"
            android:scaleType="centerInside"
            android:src="@drawable/ic_filter_list_black" />
    </LinearLayout>

    <ListView
        android:id="@+id/lv"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:cacheColorHint="@android:color/transparent"
        android:clipToPadding="false"
        android:divider="@android:color/transparent"
        android:dividerHeight="8dp"
        android:paddingStart="@dimen/project_screen_padding"
        android:paddingEnd="@dimen/project_screen_padding"
        android:paddingTop="@dimen/project_small_gap"
        android:paddingBottom="@dimen/project_small_gap" />

    <!-- Existing ad contract retained for future/conditional loading. -->
    <!-- <com.google.android.gms.ads.AdView
        android:id="@+id/adView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        ads:adSize="BANNER"
        ads:adUnitId="@string/banner_ad_unit_id" /> -->

</LinearLayout>
